function tellFortune(jobs, city, names, years) {
  // Code the function here.
  console.log(`You will be ${jobs} in ${city}, and married to ${names} in ${years} years.`);
}

// Do not remove last lines, it is for tests
module.exports = tellFortune;
