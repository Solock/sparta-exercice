// Copy your function `sentenceJoiner` from the first exercise here
export function sentenceJoiner(sentenceArray: string[]): string {
  const phrase: string = sentenceArray.join(" ");
  return phrase;
  // Code the sentenceJoiner function here
}

